{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd 'FoodHub IIT Mandi Backend' && rm -f package-lock.json && npm install"
  },
  "deploy": {
    "startCommand": "cd 'FoodHub IIT Mandi Backend' && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
